diff --git a/packages/playwright-core/src/DEPS.list b/packages/playwright-core/src/DEPS.list
index 264d291..71b8c7f 100644
--- a/packages/playwright-core/src/DEPS.list
+++ b/packages/playwright-core/src/DEPS.list
@@ -23,6 +23,15 @@ client/
 @isomorphic/**
 @utils/**
 
+# The ohos entry re-exports playwright-core itself, which resolves through
+# node_modules/playwright-core (a workspace symlink in this repo, and an npm
+# alias install in consumer projects).
+[ohos/index.ts]
+node_modules/playwright-core
+
+[ohos/utils.ts]
+"strict"
+
 [utilsBundle.ts]
 node_modules/chokidar
 node_modules/colors/safe
@@ -54,5 +63,6 @@ node_modules/yaml
 
 [serverRegistry.ts]
 "strict"
+ohos/utils.ts
 package.ts
 node_modules/chokidar
diff --git a/packages/playwright-core/src/server/DEPS.list b/packages/playwright-core/src/server/DEPS.list
index b212c0c..a0c2e3f 100644
--- a/packages/playwright-core/src/server/DEPS.list
+++ b/packages/playwright-core/src/server/DEPS.list
@@ -3,6 +3,7 @@
 @protocol/**
 @utils/**
 ../generated/
+../ohos/
 ../package.ts
 ./
 ./har/
diff --git a/packages/playwright-core/src/server/registry/DEPS.list b/packages/playwright-core/src/server/registry/DEPS.list
index a6aa9a2..e927cf7 100644
--- a/packages/playwright-core/src/server/registry/DEPS.list
+++ b/packages/playwright-core/src/server/registry/DEPS.list
@@ -1,6 +1,7 @@
 [*]
 @utils/**
 @isomorphic/**
+../../ohos/
 ../../package.ts
 ../utils.ts
 ../userAgent.ts
diff --git a/packages/playwright-core/src/tools/cli-client/DEPS.list b/packages/playwright-core/src/tools/cli-client/DEPS.list
index 07864bb..b90ad93 100644
--- a/packages/playwright-core/src/tools/cli-client/DEPS.list
+++ b/packages/playwright-core/src/tools/cli-client/DEPS.list
@@ -28,6 +28,7 @@
 
 [registry.ts]
 "strict"
+../../ohos/utils.ts
 ../../package.ts
 
 [minimist.ts]
diff --git a/utils/build/build.js b/utils/build/build.js
index 2bef516..c54451e 100644
--- a/utils/build/build.js
+++ b/utils/build/build.js
@@ -269,6 +269,19 @@ updateSteps.push(new ProgramStep({
 
 steps.push(new GroupStep(updateSteps));
 
+// Regenerate the protocol validator and channels before the esbuild steps:
+// the ohos-ports patches extend packages/protocol/spec/mixins.yml, and the
+// generated files are bundled into lib/coreBundle.js by esbuild.
+// generate_channels exits with code 1 when it rewrote files (a signal for
+// watch mode), which is the expected outcome here.
+steps.push({
+  async run() {
+    const result = child_process.spawnSync(process.execPath, ['utils/generate_channels.js'], { cwd: ROOT, stdio: 'inherit' });
+    if (result.status !== 0 && result.status !== 1)
+      throw new Error(`generate_channels exited with code ${result.status}`);
+  },
+});
+
 // Build injected icons.
 steps.push(new ProgramStep({
   command: 'node',
@@ -610,6 +623,9 @@ steps.push(new EsbuildStep({
 
     // CLI client tools, should be a separate bundle.
     filePath('packages/playwright-core/src/tools/cli-client/*.ts'),
+    // HarmonyOS entry point (also bundled into coreBundle via the server
+    // imports below).
+    filePath('packages/playwright-core/src/ohos/*.ts'),
     filePath('packages/playwright-core/src/package.ts'),
     filePath('packages/playwright-core/src/tools/utils/socketConnection.ts'),
     filePath('packages/playwright-core/src/tools/utils/extension.ts'),
@@ -911,8 +927,29 @@ const pkgSizePlugin = {
 // in other code paths (e.g. HtmlBuilder.build() reads lib/vite/htmlReport/
 // and lib/vite/traceViewer/), so we always keep the static build alongside
 // HMR. Recorder is not yet HMR'd.
-const webPackages = ['html-reporter', 'recorder', 'trace-viewer', 'dashboard'];
-for (const webPackage of webPackages) {
+// The web packages ship with the `playwright` package, not with
+// playwright-core. PW_OHOS_CORE_ONLY skips them: the ohos-ports build only
+// packages playwright-core, and skipping also avoids the vite/rolldown
+// native toolchain on openharmony.
+if (!process.env.PW_OHOS_CORE_ONLY) {
+  const webPackages = ['html-reporter', 'recorder', 'trace-viewer', 'dashboard'];
+  for (const webPackage of webPackages) {
+    steps.push(new ProgramStep({
+      command: process.execPath,
+      args: [
+        VITE_BIN,
+        'build',
+        ...(watchMode ? ['--watch', '--minify=false'] : []),
+        ...(withSourceMaps ? ['--sourcemap=inline'] : []),
+        '--clearScreen=false',
+      ],
+      shell: false,
+      cwd: path.join(__dirname, '..', '..', 'packages', webPackage),
+      concurrent: true,
+    }));
+  }
+
+  // Build/watch extension
   steps.push(new ProgramStep({
     command: process.execPath,
     args: [
@@ -923,26 +960,11 @@ for (const webPackage of webPackages) {
       '--clearScreen=false',
     ],
     shell: false,
-    cwd: path.join(__dirname, '..', '..', 'packages', webPackage),
+    cwd: path.join(__dirname, '..', '..', 'packages', 'extension'),
     concurrent: true,
   }));
 }
 
-// Build/watch extension
-steps.push(new ProgramStep({
-  command: process.execPath,
-  args: [
-    VITE_BIN,
-    'build',
-    ...(watchMode ? ['--watch', '--minify=false'] : []),
-    ...(withSourceMaps ? ['--sourcemap=inline'] : []),
-    '--clearScreen=false',
-  ],
-  shell: false,
-  cwd: path.join(__dirname, '..', '..', 'packages', 'extension'),
-  concurrent: true,
-}));
-
 // Generate CLI help.
 onChanges.push({
   inputs: [
@@ -1069,6 +1091,25 @@ copyFiles.push({
   to: 'packages/playwright-core/lib',
 });
 
+// Ship the playwright-ohos license next to the HarmonyOS entry point.
+copyFiles.push({
+  files: 'packages/playwright-core/src/ohos/*.LICENSE',
+  from: 'packages/playwright-core/src',
+  to: 'packages/playwright-core/lib',
+});
+
+// The npm pack of playwright-core ships the root license files.
+copyFiles.push({
+  files: 'LICENSE',
+  from: '.',
+  to: 'packages/playwright-core',
+});
+copyFiles.push({
+  files: 'NOTICE',
+  from: '.',
+  to: 'packages/playwright-core',
+});
+
 if (watchMode) {
   // Run TypeScript for type checking.
   steps.push(new ProgramStep({
